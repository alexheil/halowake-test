<% if @user.albums.any? %>
  <div id="albums-tab" class="two-column center tab">
    Albums
  </div>
  <div id="feed-tab" class="two-column center tab">
    Feed
  </div>
<% elsif @user.photos.any? || @user.following.count > 0 %>
  <div class="nameplate center border-bottom">
    Feed
  </div>
<% end %>